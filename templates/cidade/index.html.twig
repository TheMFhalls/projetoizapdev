{% extends 'base.html.twig' %}

{% block title %}Cidade index{% endblock %}

{% block body %}
    <h1>Cidade index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cidade in cidades %}
            <tr>
                <td>{{ cidade.id }}</td>
                <td>{{ cidade.nome }}</td>
                <td>
                    <a href="{{ path('cidade_show', {'id': cidade.id}) }}">show</a>
                    <a href="{{ path('cidade_edit', {'id': cidade.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cidade_new') }}">Create new</a>
{% endblock %}